# 🎙️ VoiceOps Frontend

This is the web dashboard for VoiceOps.

## 🚀 Deployment Instructions (Vercel)

1. Connect this repo to Vercel.
2. Under **Environment Variables**, add:
   - `NEXT_PUBLIC_API_URL`: `https://voiceops-backend.onrender.com`
3. Click **Deploy**.

## 🛠️ Local Development
1. Create a `.env.local` file:
   ```env
   NEXT_PUBLIC_API_URL=https://voiceops-backend.onrender.com
   ```
2. Run `npm run dev`.
